import type { Process } from "../../process";
import type { DataModel } from "../../resources";
export declare function process(options: {
    transactionHash: string;
}): Process<DataModel.Block | undefined, {
    web3: "eth_getTransactionByHash";
}>;
